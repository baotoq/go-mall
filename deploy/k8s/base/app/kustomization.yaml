apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - greeter.yaml
  - rbac.yaml
configMapGenerator:
  - name: greeter-config
    files:
      - config.yaml=../../../../app/greeter/configs/config.yaml
generatorOptions:
  disableNameSuffixHash: true
